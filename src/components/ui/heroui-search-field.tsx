// HeroUI's stylesheet (@heroui/styles) is imported once in src/index.css, right
// after Tailwind, so it shares the site's single Tailwind build.
import { SearchField } from '@heroui/react'

export { SearchField }
export default SearchField
