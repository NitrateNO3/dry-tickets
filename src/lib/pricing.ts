// ponytail: placeholder rate (see README) — replace with the real fee schedule.
// Shared by the event page and api/booking.ts, so the emails always match what the buyer saw.
export const BOOKING_FEE_RATE = 0.045
export const MAX_TICKETS = 10
